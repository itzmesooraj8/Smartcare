[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
# Add any environment variables you need for production here, for example:
#  VITE_API_URL = "https://api.example.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
